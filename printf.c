#include "holberton.h"

/**
 *
 *
 *
 *
 **/
int _printf(const char *format, ...)
{
	/*some int variable to represent the num of chars
	printed excluding the null byte*/

	/*use typedef'd ops_t list array thing from header file for each flag.
	something like 
		{"c", char_prnt_function}
		{"s", str_prnt_function}
	I'm still fuzzy on how to do that, but I think we can figure it out.*/
}
